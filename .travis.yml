language: python
python:
  - 3.7
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install codecov
install:
  - pip install --editable .
script:
  - pallets cov
  - pallets flake8
  - pallets test
after_success:
  - codecov
